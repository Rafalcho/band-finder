import React from 'react';
import {Link} from 'react-router-dom';

export const Nav = () => {
  return (
    <div className='search-again'>
      <Link to='/' >Home</Link>
    </div>
    );
};

export default Nav;
